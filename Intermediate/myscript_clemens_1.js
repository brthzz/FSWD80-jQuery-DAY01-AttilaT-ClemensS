$("form").submit(function(e) {

	e.preventDefault();
	

});

// $("#myform").remove();

$(document).ready(function() {
			for (i = 0; i < 5; i++)
					{
            		var newItem = '<div><p>test</p></div>'; // definition of the new element
            		$('article').append(newItem); 
            		}
            		// insert the new element before the <li> tag
        	
 $("div").addClass("box");
        	});

$("input").val("Search for ...");

 $("footer > p > a").prop("href", "http://www.codefactory.wien/");